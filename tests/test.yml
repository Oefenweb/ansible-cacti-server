# test file for cacti-server
---
- hosts: localhost
  connection: local
  become: true
  pre_tasks:
    - name: include vars
      include_vars: main.yml
    - include: pre.yml
  roles:
    - ../../
  handlers:
    - include: handlers/main.yml
  vars:

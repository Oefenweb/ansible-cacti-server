# test file for cacti-server
---
- hosts: localhost
  connection: local
  become: true
  pre_tasks:
    - debug:
        msg: "{{ ansible_user_id }}"
    - debug:
        msg: "{{ lookup('env','USER') }}"
    - include_vars: main.yml
    - include: pre.yml
  roles:
    - ../../
  handlers:
    - include: handlers/main.yml
  vars:
